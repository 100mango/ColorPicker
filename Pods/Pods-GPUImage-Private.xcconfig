#include "Pods-GPUImage.xcconfig"
GCC_PREPROCESSOR_DEFINITIONS = COCOAPODS=1
HEADER_SEARCH_PATHS = "${PODS_ROOT}/BuildHeaders" "${PODS_ROOT}/BuildHeaders/GPUImage" "${PODS_ROOT}/Headers" "${PODS_ROOT}/Headers/GPUImage"
OTHER_LDFLAGS = -ObjC ${PODS_GPUIMAGE_OTHER_LDFLAGS}
PODS_ROOT = ${SRCROOT}